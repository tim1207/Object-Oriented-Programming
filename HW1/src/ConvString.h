#include <iostream>
#include <string>
#include <ctype.h>

using namespace std;
string printOUT(string input,int index,int num,int *index_out);
string convert(string input);
int checkInput(string input);
